{
  "name": "quest-update",
  "version": "0.1",
  "description": "Update Quests App",
  "main": "./src/main.js",
  "scripts": {
    "electron": "electron src/main.js",
    "start": "set NODE_ENV=dev && nodemon --watch ./src/**/* --watch . --exec yarn electron"
  },
  "author": "exutec",
  "license": "MIT",
  "devDependencies": {
    "electron": "^15.5.5"
  },
  "dependencies": {
    "nodemon": "^1.19.1"
  }
}
